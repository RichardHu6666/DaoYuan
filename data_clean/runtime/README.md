# runtime

This directory is private to the `data_clean` sub-application.

- `process_state.sqlite3`: sidecar state database
- `raw_cache/`: cached `raw_html` and `original_text`
- `logs/`: operational logs
